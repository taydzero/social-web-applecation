# Руководство по запуску проекта

## ✅ Статус бэкенда

Бэкенд успешно запущен и работает:
- ✅ PostgreSQL подключен
- ✅ Таблицы созданы (`user` и `message`)
- ✅ Сервер запущен на порту 5000
- ✅ Все контроллеры настроены для работы с фронтендом

## 🚀 Запуск фронтенда

### Шаг 1: Убедитесь, что бэкенд запущен
Бэкенд должен работать на `http://localhost:5000`

### Шаг 2: Запустите фронтенд

Откройте **новый терминал** (бэкенд должен продолжать работать) и выполните:

```bash
# В корне проекта (vk-copy)
npm start
```

Фронтенд запустится на `http://localhost:3000` и автоматически откроется в браузере.

## 📋 Что проверить после запуска

### 1. Регистрация нового пользователя
- Перейдите на `/register`
- Создайте аккаунт
- После регистрации вы должны быть автоматически авторизованы

### 2. Вход в систему
- Перейдите на `/login`
- Войдите с созданными учетными данными

### 3. Профиль пользователя
- После входа проверьте, что профиль загружается
- URL: `/api/users/profile` должен возвращать данные пользователя

### 4. Сообщения
- Перейдите на `/Message` - должен показаться список переписок
- Если переписок нет, создайте нового пользователя и отправьте сообщение

### 5. Отправка сообщений
- Перейдите к профилю другого пользователя
- Нажмите "Отправить сообщение"
- Проверьте, что сообщения отправляются и отображаются

## 🔍 Проверка в браузере

Откройте **Developer Tools** (F12) и проверьте:

1. **Console** - не должно быть ошибок
2. **Network** - запросы к `http://localhost:5000` должны возвращать статус 200
3. **Application/Storage** - токен должен сохраняться в `localStorage`

## 🐛 Возможные проблемы

### Проблема: "Network Error" или CORS ошибки
**Решение**: Убедитесь, что бэкенд запущен на порту 5000 и CORS настроен правильно

### Проблема: "401 Unauthorized"
**Решение**: 
- Проверьте, что токен сохраняется в localStorage
- Попробуйте выйти и войти снова

### Проблема: "Пользователь не найден"
**Решение**: 
- Убедитесь, что пользователь зарегистрирован
- Проверьте, что ID передается правильно (должен быть строкой)

### Проблема: Сообщения не отображаются
**Решение**:
- Проверьте формат данных в Network tab
- Убедитесь, что данные приходят в формате `{ _id, from, to, content, timestamp }`

## 📝 Структура данных

### Формат пользователя (ожидается фронтендом):
```json
{
  "_id": "1",
  "name": "Имя",
  "email": "email@example.com",
  "bio": "О себе",
  "avatar": "/uploads/avatars/...",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z"
}
```

### Формат сообщения (ожидается фронтендом):
```json
{
  "_id": "1",
  "from": { "_id": "1", "name": "Отправитель", ... },
  "to": { "_id": "2", "name": "Получатель", ... },
  "content": "Текст сообщения",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

## ✅ Все готово!

Если все работает:
1. ✅ Бэкенд запущен на порту 5000
2. ✅ Фронтенд запущен на порту 3000
3. ✅ Регистрация и вход работают
4. ✅ Сообщения отправляются и получаются
5. ✅ Профили пользователей отображаются

**Проект готов к использованию!** 🎉

